<%= form_with(model: candidate, local: true) do |form| %>
  <% if candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% candidate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="actions">
      <br>
      <%= form.label :image, "Avatar"%><br>
      <button class="favth-btn">
        <%= form.file_field :image, class: "btn btn-large btn-primary" %>
        <% if form.object.image? %>
            <%= image_tag form.object.image.thumb.url %>

            <%= form.label :remove_image %>
            <%= form.check_box :remove_image %>
        <% end %>
      </button >
    </div>



  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :candidate_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :candidate_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

    <p>
      <%= link_to 'Delete Candidate', @candidate, method: :delete, data: { confirm: 'Are you sure?' } %>
    </p>

<% end %>
